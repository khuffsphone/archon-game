# Game slice target platform

The Part 1 combat slice is **browser-first**.

Use:
- React + Vite web runtime
- approved exported asset files
- browser-safe image, audio, and input behavior

Do not:
- switch to React Native
- switch to Expo
- use Workshop preview memory as runtime data

The goal is one real web combat slice that proves the export contract.
